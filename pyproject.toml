[project]
name = "oxzoo-worker-python"
version = "0.1.0"
description = "Python worker example for the ox control plane: consumes a Redis queue and records each completed job in Postgres."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg[binary]==3.3.5",
    "redis==6.4.0",
]

[tool.uv]
package = false
